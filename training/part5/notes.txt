https://roboticsbackend.com/what-is-a-ros-node/


sudo docker start -t <exited container_name>


*** PART 1 ***
Create a Docker development environment and then create a catkin workspace. Develop a ROS package that has both Python and C++ nodes and build it.


Run docker image: ros (is ubuntu+ros), once container is up, exec -it <container>
Inside the container, set up the workspace.


[container -it, no Dockerfile involved]

sudo docker pull ros
sudo docker run -it ros bash

sudo sh -c 'echo "deb http://packages.ros.org/ros/ubuntu $(lsb_release -sc) main" > /etc/apt/sources.list.d/ros-latest.list'
sudo apt-key adv --keyserver 'hkp://keyserver.ubuntu.com:80' --recv-key C1CF6E31E6BADE8868B172B4F42ED6FBAB17C654
sudo apt update
sudo apt-get install ros-noetic-catkin
sudo apt-get install cmake python3-catkin-pkg python3-empy python-nose python-setuptools libgtest-dev build-essential
sudo apt-get install ros-noetic-roscpp ros-noetic-rospy

source /opt/ros/noetic/setup.bash	# Came default with foxy, now have noetic
echo "source /opt/ros/noetic/setup.bash" >> ~/.bashrc
source ~/.bashrc


mkdir -p ~/catkin_ws/src	# Make workspace
cd ~/catkin_ws/
catkin_make			# 1st time will create CMakeLists.txt in /src

***source devel/setup.bash	# Overlay workspace with setup script
***echo $/home/alexng/catkin_ws/src:/opt/ros/noetic/share


<make package with dependencies of Python and C++>
cd ~/catkin_ws/src
catkin_create_pkg first_pkg roscpp rospy std_msgs
cd ~/catkin_ws
catkin_make
. ~/catkin_ws/devel/setup.bash

<build that package>










[terminal]
sudo docker pull ros
sudo docker run -it ros bash

source ros_entrypoint.sh

sudo apt-get install ros-noetic-catkin
sudo apt-get install cmake python-catkin-pkg python-empy python-nose python-setuptools libgtest-dev build-essential
mkdir build && cd build && cmake ../ && make && sudo make install

mkdir -p ~/catkin_ws/src
cd ~/catkin_ws/
catkin_make

[new if use dockerfile]




 # Catkin (A build system, will use to create packages)
 
 INSTALLATION
 sudo apt-get install ros-noetic-catkin
 sudo apt-get install cmake python-catkin-pkg python-empy python-nose python-setuptools libgtest-dev build-essential
 mkdir build && cd build && cmake ../ && make && sudo make install
 
 WORKSPACE
 $ mkdir -p ~/catkin_ws/src
 $ cd ~/catkin_ws/
 $ catkin_make
 
 
 
 # Packages
 
 Catkin packages consists of CMakeLists.txt, package.xml
 
 catkin_create_pkg <package_name> [dep1] [dep2]
 
 
 
 # Notes
 rospack find <package name>
 roscd <package name>		To change to a package dir
 pwd		Print working dir
 
 
 
*** PART 2 ***

